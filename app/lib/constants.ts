/** App title — displayed in the nav header and browser tab */
export const APP_TITLE = "FOI Sentinel"

/** Path to the logo in /public (used in the header and as favicon) */
export const LOGO_SRC = "/snowflake-logo.png"

/** Fully-qualified schema holding the FOI data model. */
export const SCHEMA = "FOI.FOI_SENTINEL_V2"

/** Schema where the Openflow SharePoint connector lands the live SAR corpus. */
export const SAR_INGEST_SCHEMA = "FOI.SAR_INGEST"
